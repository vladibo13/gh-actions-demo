name: Mattrix Configuration

on:
    push:
    workflow_dispatch:

jobs:
    deploy-on-ubuntu-job:
        strategy:
            matrix:
                os: [ubuntu-latest, ubuntu-20.04, windows-latest]
                images: [hello-world, alpine]
        runs-on: ${{matrix.os}}
        steps:
          - name: Echo docker info
            run: docker info

          - name: Run Image on ${{matrix.os}}
            run: docker run ${{matrix.images}}   

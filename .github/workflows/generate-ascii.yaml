name: Generate Ascii

on: push

jobs:
  ascii_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: List Files
        run: ls -ltra   

      - name: Run Script
        run: |
          chmod +x script.sh
         ./script.sh         

